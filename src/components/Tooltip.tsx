import React from 'react';\n\ninterface TooltipProps {\n  children: React.ReactNode;\n  show: boolean;\n  x: number;\n  y: number;\n}\n\nconst Tooltip: React.FC<TooltipProps> = ({ children, show, x, y }) => {\n  if (!show) return null;\n  \n  return (\n    <div\n      className=\"absolute bg-white text-black shadow-md rounded-lg p-3 z-50 min-w-[200px]\"\n      style={{\n        left: x,\n        top: y,\n        transform: 'translate(-50%, -100%)',\n        pointerEvents: 'none',\n        borderColor: '#e5e7eb',\n        borderWidth: '1px',\n      }}\n    >\n      <div className=\"relative\">\n        {children}\n        {/* Triangle pointer */}\n        <div\n          className=\"absolute w-3 h-3 bg-white rotate-45 border-b border-r border-gray-200\"\n          style={{ bottom: '-10px', left: 'calc(50% - 6px)' }}\n        />\n      </div>\n    </div>\n  );\n};\n\nexport default Tooltip;